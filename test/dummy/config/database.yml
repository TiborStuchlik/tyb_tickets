# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  encoding: utf8
  username: root
  password: tibaba
  host: localhost
  port: 3306
  ssl_mode: :disabled
  sslverify: false

development:
  <<: *default
  database: tyb

test:
  <<: *default
  database: tyb


production:
  <<: *default
  database: tyb
